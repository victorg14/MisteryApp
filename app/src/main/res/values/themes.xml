<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Mistery_app" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Mistery_app" parent="Base.Theme.Mistery_app" />

    <style name="Theme.App.Splash" parent="Theme.Mistery_app">
        <item name="android:windowBackground">@color/mystery_dark</item>
        <item name="android:windowDisablePreview">true</item>
    </style>
</resources>